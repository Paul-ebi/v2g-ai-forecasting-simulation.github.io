title: "V2G AI Forecasting & Simulation"
description: "Documentation for load, PV, EV forecasting and MATLAB-based V2G simulation."
remote_theme: just-the-docs/just-the-docs

# Optional site settings
color_scheme: dark    # or "light"
search_enabled: true

# Markdown settings
markdown: kramdown
highlighter: rouge

# Navigation structure (optional — sidebar auto-generates)
nav_order: 1

# Exclude unnecessary folders
exclude:
  - venv/
  - data/
  - notebooks/
  - .gitignore
  - README.md

# Logo example (optional)
# logo: "/assets/logo.png"
